{
  "name": "launchkit-ai",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"Define dev script in later versions\"",
    "test": "vitest run",
    "lint": "echo \"Define lint script in later versions\""
  },
  "devDependencies": {
    "vitest": "^2.0.5",
    "@vitest/ui": "^2.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4",
    "@types/node": "^20.14.9"
  }
}
