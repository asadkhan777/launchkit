{
  "name": "@launchkit-ai/tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --config=vitest.config.ts",
    "test:watch": "vitest --config=vitest.config.ts",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@launchkit-ai/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
